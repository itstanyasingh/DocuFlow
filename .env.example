# DOCUFLOW VERCEL UNIFIED DEPLOYMENT CONFIGURATION
# Frontend and API routes run natively on the same Vercel deployment.
# No backend URL configuration (VITE_API_BASE_URL) or CORS URLs are required.

# GEMINI_API_KEY: Optional server-side AI processing key
GEMINI_API_KEY=
